# Pinned, so that a rerun in two years produces the same numbers.
stg17 @ git+https://github.com/STG17-Africa/stg17-workshop
numpy>=1.24
pandas>=2.0
matplotlib>=3.7
geopandas>=0.14
rasterio>=1.3
h5py>=3.9
pyarrow>=14.0
scipy>=1.11
